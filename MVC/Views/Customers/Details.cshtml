@model MVC.Models.Customer
@{
	ViewBag.Title = "Details";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Model.Id == 0)
{
	<h2>There are no customers with that Id</h2>
}
else
{
	<h2>@Model.Name</h2>
}
